import React from "react";
import Box from "@mui/material/Box";
import { FarmType } from "../models";

export default function FarmContent(props: { item: FarmType }) {
  return <Box sx={{ height: 200 }}></Box>;
}
